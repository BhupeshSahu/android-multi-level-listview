apply plugin: 'com.android.library'

android {

    compileSdkVersion 29

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 29
        versionCode 1
        versionName "1.0.1"
    }

}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
